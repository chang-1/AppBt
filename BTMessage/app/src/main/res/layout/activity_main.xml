<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity">

    <com.google.android.material.appbar.AppBarLayout
            android:layout_height="wrap_content"
            android:layout_width="match_parent"
            android:theme="@style/AppTheme.AppBarOverlay">

    </com.google.android.material.appbar.AppBarLayout>

    <include layout="@layout/content_main"/>

    <Button
            android:text="Connect"
            android:layout_width="100dp"
            android:layout_height="60dp" android:id="@+id/buttonConnect" android:layout_marginStart="110dp"/>
    <Button
            android:text="Scan"
            android:layout_width="100dp"
            android:layout_height="60dp" android:id="@+id/buttonScan"/>
    <Spinner
            android:layout_width="match_parent"
            android:layout_height="60dp" android:id="@+id/spinnerDeviceSelect" android:layout_marginTop="70dp"/>
    <Button
            android:text="Send message"
            android:layout_width="match_parent"
            android:layout_height="60dp" android:id="@+id/buttonMessage" android:layout_marginTop="140dp"/>

</androidx.coordinatorlayout.widget.CoordinatorLayout>